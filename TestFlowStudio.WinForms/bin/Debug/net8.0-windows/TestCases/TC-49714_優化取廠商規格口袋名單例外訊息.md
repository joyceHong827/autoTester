---
redmine_issue_id: 49714
title: 優化取廠商規格口袋名單例外訊息
status: pending
last_run: ''
created_at: 2026-05-06T11:37:34.3235355+08:00
playwright_script: ./GeneratedScripts\TC-49714_優化取廠商規格口袋名單例外訊息.spec.ts
---

##測試目的
驗證系統在擷取廠商規格口袋名單發生例外時，錯誤日誌是否正確記錄使用者 ID 與帳號名稱。

##測試步驟
1.  登入系統並操作至會觸發 `ProductSpecificationService.GetSpecPackOfVendor` 方法的功能（例如：嘗試從某處載入廠商規格口袋名單）。
2.  透過設定或模擬方式，確保 `ProductSpecificationService.GetSpecPackOfVendor` 方法在執行期間會拋出例外，使其進入 `catch` 區塊。
3.  檢查應用程式的錯誤日誌系統，確認是否有新的錯誤記錄產生。
4.  檢查使用者介面，確認畫面上顯示的錯誤訊息。

##測試資料
*   具有有效 `user.id` 的使用者帳號。
*   該使用者帳號已設定 `user.accountName` (公司名稱)。
    *   範例：
        *   `user.id`: 32230
        *   `user.accountName`: "eHub工廠店：全渠運購股份有限公司"

##預期結果
1.  錯誤日誌中會記錄一條包含以下格式的錯誤訊息：
    `取廠商規格口袋名單 ERROR，user id:{user.id} ({user.accountName})`
    （例如：「取廠商規格口袋名單 ERROR，user id:32230 (eHub工廠店：全渠運購股份有限公司)」）
2.  使用者介面會顯示通用的錯誤提示訊息：「讀取自訂規格群組失敗」。

##實際結果
<!-- 由系統自動填入 -->

##驗證項目
| 驗證項目 | 預期結果 |
| :------------------------------- | :------- |
| 日誌中是否包含 "user id:" 關鍵字 | 是       |
| 日誌中是否包含正確的 User ID     | 是       |
| 日誌中是否包含括號 `()`          | 是       |
| 括號中是否包含正確的 Account Name | 是       |
| 日誌中是否包含 "取廠商規格口袋名單 ERROR" | 是       |
| 使用者介面是否顯示 "讀取自訂規格群組失敗" | 是       |

##測試結論
<!-- 由系統自動填入 -->