---
redmine_issue_id: 49299
title: 新增IB員購設定
status: pending
last_run: ''
created_at: 2026-05-06T17:43:47.4426636+08:00
playwright_script: ./GeneratedScripts/TC-49299_新增IB員購設定.spec.ts
---

##測試目的
驗證B2E「維護大批商品」功能中，針對IB商品設定員購項目時，是否正確呼叫對應的API並傳送正確的ChannelCode (86)，以及資料庫紀錄是否正確建立與刪除，同時確保OB商品員購設定的現有功能不受影響。

##測試步驟
1.  開啟瀏覽器，導航至B2E商品管理 ＞ 維護大批商品 ＞ 商品欄位頁面 (`https://b2e.etmall.com.tw/Web/BatchUpdateProduct`)。
2.  在商品ID欄位輸入一個IB商品ID (etype=8)，點擊查詢。
3.  在查詢結果的「員購項目」下拉選單中選擇「是」。
4.  點擊「儲存」或「確認」按鈕。
5.  重複步驟1，在商品ID欄位輸入同一個IB商品ID (etype=8)，點擊查詢。
6.  在查詢結果的「員購項目」下拉選單中選擇「否」。
7.  點擊「儲存」或「確認」按鈕。
8.  重複步驟1，在商品ID欄位輸入一個OB商品ID，點擊查詢。
9.  在查詢結果的「員購項目」下拉選單中選擇「是」。
10. 點擊「儲存」或「確認」按鈕。
11. 重複步驟1，在商品ID欄位輸入同一個OB商品ID，點擊查詢。
12. 在查詢結果的「員購項目」下拉選單中選擇「否」。
13. 點擊「儲存」或「確認」按鈕。

##測試資料
*   **IB商品ID (etype=8):** [請在此處填入一個實際的IB商品ID]
*   **OB商品ID:** [請在此處填入一個實際的OB商品ID]

##預期結果
1.  完成步驟4後：
    *   B2E應呼叫 `Product/CreateStaffProduct` API。
    *   該API請求的 `ChannelCode` 欄位值應為 `86`。
    *   資料庫 `ET1ETMall..tb_Product_StaffPurchase` 應新增一筆對應此IB商品ID的紀錄，且 `ChannelCode` 為 `86`。
2.  完成步驟7後：
    *   B2E應呼叫 `Product/DeleteStaffProduct` API。
    *   資料庫 `ET1ETMall..tb_Product_StaffPurchase` 應移除對應此IB商品ID的紀錄。
3.  完成步驟10後：
    *   B2E應呼叫 `Product/CreateStaffProduct` API。
    *   該API請求的 `ChannelCode` 欄位值應為 `84`。
    *   資料庫 `ET1ETMall..tb_Product_StaffPurchase` 應新增一筆對應此OB商品ID的紀錄，且 `ChannelCode` 為 `84`。
4.  完成步驟13後：
    *   B2E應呼叫 `Product/DeleteStaffProduct` API。
    *   資料庫 `ET1ETMall..tb_Product_StaffPurchase` 應移除對應此OB商品ID的紀錄。

##實際結果
<!-- 由系統自動填入 -->

##驗證項目
| 驗證項目                                 | 預期行為                                                              | 實際行為 |
| :--------------------------------------- | :-------------------------------------------------------------------- | :------- |
| **IB商品：設定員購**                     |                                                                       |          |
| 呼叫API正確性                            | 呼叫 `Product/CreateStaffProduct` API。                               |          |
| API參數 ChannelCode 正確性               | `ChannelCode` 值為 `86`。                                             |          |
| 資料庫紀錄新增及 ChannelCode 正確性      | `ET1ETMall..tb_Product_StaffPurchase` 新增紀錄，`ChannelCode` 為 `86`。 |          |
| **IB商品：移除員購**                     |                                                                       |          |
| 呼叫API正確性                            | 呼叫 `Product/DeleteStaffProduct` API。                               |          |
| 資料庫紀錄移除正確性                     | `ET1ETMall..tb_Product_StaffPurchase` 移除對應紀錄。                  |          |
| **OB商品：設定員購 (回歸測試)**          |                                                                       |          |
| 呼叫API正確性                            | 呼叫 `Product/CreateStaffProduct` API。                               |          |
| API參數 ChannelCode 正確性               | `ChannelCode` 值為 `84`。                                             |          |
| 資料庫紀錄新增及 ChannelCode 正確性      | `ET1ETMall..tb_Product_StaffPurchase` 新增紀錄，`ChannelCode` 為 `84`。 |          |
| **OB商品：移除員購 (回歸測試)**          |                                                                       |          |
| 呼叫API正確性                            | 呼叫 `Product/DeleteStaffProduct` API。                               |          |
| 資料庫紀錄移除正確性                     | `ET1ETMall..tb_Product_StaffPurchase` 移除對應紀錄。                  |          |

##測試結論
<!-- 由系統自動填入 -->